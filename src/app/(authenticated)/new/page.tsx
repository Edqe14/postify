import { NewForm } from './form';

export default function NewPage() {
  return (
    <section className="container py-12 flex-grow flex flex-col gap-4 border-x">
      <NewForm />
    </section>
  );
}
